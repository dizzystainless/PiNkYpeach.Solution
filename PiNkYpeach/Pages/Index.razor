@page "/"
@using PiNkYpeach.Api.Pages
@using MudBlazor

<PageTitle>Index</PageTitle>

<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="PinkTheme" />

<MudToggleIconButton @bind-Toggled="@_isDarkMode"
                     Icon="@Icons.Material.Filled.DarkMode" Color="@Color.Default"
                     ToggledIcon="@Icons.Material.Filled.WbSunny" ToggledColor="@Color.Default"/>

@* <MudButton Variant="Variant.Filled" Color="Color.Secondary">login</MudButton> *@


@code{
    private bool _isDarkMode;

    MudTheme PinkTheme = new MudTheme()
    {
        Palette = new PaletteLight()
        {
            Primary = Colors.Pink.Darken2,
            Secondary = Colors.Pink.Accent4,
            AppbarBackground = Colors.Pink.Darken3,
            Background = Colors.Pink.Lighten4,
            
            //Define other properties here.
        },
            PaletteDark = new PaletteDark()
        {
            Primary = Colors.Purple.Darken2,
            Secondary = Colors.Purple.Accent4,
            AppbarBackground = Colors.Purple.Darken3,
            Background = Colors.Purple.Lighten4,
            //Define other properties here.
        }
        
    };
}   
